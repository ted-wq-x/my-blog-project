<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wq.website.dao.UserMapper">

    <select id="findByName" resultType="com.wq.website.modal.User" parameterType="String">
        SELECT *
        FROM t_users
        WHERE username = #{name};
    </select>

    <insert id="insertUserVo" parameterType="com.wq.website.modal.User" useGeneratedKeys="true" keyProperty="uid">
        INSERT INTO t_users (username, password, email, home_url, screen_name, created, activated, logged, group_name)
        VALUES (#{username}, #{password}, #{email}, #{home_url}, #{screen_name}, #{created}, #{activated},
                #{logged}, #{group_name});
    </insert>

</mapper>